<Window x:Class="GameLauncher.UI.DynamicIpChangerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dynamic IP Changer" Height="620" Width="680"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Foreground="#CDD6F4"
        FontFamily="Segoe UI, Inter, Cascadia Code, sans-serif"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#89B4FA"/>
            <Setter Property="Foreground" Value="#11111B"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" TargetName="border" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogCard" TargetType="Border">
            <Setter Property="Background" Value="#181826"/>
            <Setter Property="BorderBrush" Value="#2B2B3F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>
    </Window.Resources>

    <Border x:Name="DialogBorderContainer" Background="#11111B" BorderBrush="#313244" BorderThickness="1" CornerRadius="16" Margin="10" Opacity="0.0" RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform x:Name="scaleDialog" ScaleX="0.85" ScaleY="0.85"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="4" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <!-- Titlebar -->
                <RowDefinition Height="Auto"/>
                <!-- Body -->
                <RowDefinition Height="*"/>
                <!-- Footer -->
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- TITLEBAR -->
            <Grid Grid.Row="0" Margin="0,0,0,16" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Background="Transparent">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🔄" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Dynamic IP Changer" FontSize="20" FontWeight="Bold" Foreground="#89B4FA" VerticalAlignment="Center"/>
                </StackPanel>

                <Button x:Name="btnCloseIcon" Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center" 
                        Foreground="#A6ADC8" Background="Transparent" BorderThickness="0" FontSize="14" FontWeight="Bold" 
                        Cursor="Hand" Click="btnClose_Click"/>
            </Grid>

            <!-- CONTENT BODY -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Vertical">
                    <!-- EXPLANATION CARD -->
                    <Border Style="{StaticResource DialogCard}" BorderBrush="#89B4FA">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="💡" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="How IP Refreshing Works" FontSize="14" FontWeight="Bold" Foreground="#89B4FA"/>
                            </StackPanel>
                            <TextBlock Text="Network Settings attempts to reset your network configuration and request a new IP from your internet provider." 
                                       FontSize="13" Foreground="#CDD6F4" TextWrapping="Wrap" Margin="0,0,0,6" LineHeight="18"/>
                            <TextBlock Text="Some internet providers may not assign a new IP immediately." 
                                       FontSize="13" Foreground="#FAB387" TextWrapping="Wrap" Margin="0,0,0,6" FontWeight="SemiBold"/>
                            <TextBlock Text="If the IP does not change, try the following manual method:" 
                                       FontSize="13" Foreground="#CDD6F4" TextWrapping="Wrap" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <!-- TUTORIAL STEPS CARD -->
                    <Border Style="{StaticResource DialogCard}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <TextBlock Text="📋" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Manual Router Power-Cycle Tutorial" FontSize="14" FontWeight="Bold" Foreground="#CBA6F7"/>
                            </StackPanel>

                            <StackPanel Margin="6,0,0,0">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <Border Background="#2A2A3D" CornerRadius="10" Width="22" Height="22" Margin="0,0,10,0" VerticalAlignment="Center">
                                        <TextBlock Text="1" FontSize="11" FontWeight="Bold" Foreground="#89B4FA" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="Turn off your router / modem." FontSize="13" Foreground="#CDD6F4" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <Border Background="#2A2A3D" CornerRadius="10" Width="22" Height="22" Margin="0,0,10,0" VerticalAlignment="Center">
                                        <TextBlock Text="2" FontSize="11" FontWeight="Bold" Foreground="#89B4FA" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="Disconnect the power cable from the router." FontSize="13" Foreground="#CDD6F4" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <Border Background="#2A2A3D" CornerRadius="10" Width="22" Height="22" Margin="0,0,10,0" VerticalAlignment="Center">
                                        <TextBlock Text="3" FontSize="11" FontWeight="Bold" Foreground="#89B4FA" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="Wait at least 15 seconds." FontSize="13" Foreground="#FAB387" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <Border Background="#2A2A3D" CornerRadius="10" Width="22" Height="22" Margin="0,0,10,0" VerticalAlignment="Center">
                                        <TextBlock Text="4" FontSize="11" FontWeight="Bold" Foreground="#89B4FA" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="Plug the power cable back in." FontSize="13" Foreground="#CDD6F4" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <Border Background="#2A2A3D" CornerRadius="10" Width="22" Height="22" Margin="0,0,10,0" VerticalAlignment="Center">
                                        <TextBlock Text="5" FontSize="11" FontWeight="Bold" Foreground="#89B4FA" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="Wait until your internet connection is fully restored." FontSize="13" Foreground="#CDD6F4" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                    <Border Background="#2A2A3D" CornerRadius="10" Width="22" Height="22" Margin="0,0,10,0" VerticalAlignment="Center">
                                        <TextBlock Text="6" FontSize="11" FontWeight="Bold" Foreground="#A6E3A1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="Check if your public IP address has changed." FontSize="13" Foreground="#A6E3A1" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- ADDITIONAL ISP INFO NOTICE -->
                    <Border Style="{StaticResource DialogCard}" Background="#14141E">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="ℹ️" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="ISP Configuration Note" FontSize="12" FontWeight="Bold" Foreground="#A6ADC8"/>
                            </StackPanel>
                            <TextBlock Text="The Dynamic IP Changer does not directly control your ISP. It only helps trigger a network refresh. The final IP change depends on your internet provider configuration." 
                                       FontSize="12" Foreground="#6C7086" TextWrapping="Wrap" LineHeight="16"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- FOOTER CLOSE BUTTON -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button x:Name="btnClose" Content="Close" Style="{StaticResource ModernButton}" Click="btnClose_Click" Width="110"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
